{
  "name": "dsh-office-reader",
  "version": "0.1.0",
  "description": "DSH Office read + generate: read Word/Excel/PowerPoint (docx/xlsx/pptx and legacy doc/xls/ppt) into text/tables in memory (no intermediate files, model on-demand), and generate styled Word (.docx) from Markdown (md2docx moved here). | DSH 插件：Office 读取 + 生成 —— 内存读取 Word/Excel/PPT 内容为文本/表格(无中间文件、模型按需用)，并可 Markdown 生成带页码 Word。",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./client/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "lib/md2docx.py",
    "client",
    "cordis.patch.yml",
    "dsh.plugin.json"
  ],
  "dependencies": {
    "mammoth": "^1.12.1",
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.5",
    "word-extractor": "^1.0.4",
    "jszip": "^3.10.1"
  },
  "peerDependencies": {
    "@deepseek-ai/dsh-tools": "*"
  },
  "peerDependenciesMeta": {
    "@deepseek-ai/dsh-tools": {
      "optional": true
    }
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-connection",
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-locale",
        "@deepseek-ai/dsh-client-ui-slots",
        "@deepseek-ai/dsh-client-ui-settings",
        "@deepseek-ai/dsh-client-ui-theme",
        "@deepseek-ai/dsh-client-ui-conversation",
        "@deepseek-ai/dsh-client-ui-input-trigger",
        "@deepseek-ai/dsh-client-ui-layout"
      ],
      "platform": "web"
    }
  }
}
