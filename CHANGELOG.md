## v0.1.5 (2026-09-20)

### 修复
- **生成的 docx 纸张改为 A4（210×297mm）**：此前 `md2docx` 从未设置纸张，python-docx 的默认模板是 **US Letter**（215.9×279.4mm）—— 中文公文/专利文件的纸张与页数都会与预期不符。现在显式设为 A4。
- **页脚改为「第 1 页」**：数字左右**各一个半角空格**（此前用别的空格会被渲染器吞掉，出现「第 1页」这类偏位）。
- **页码域与缓存数字分在不同 run**：此前把「域字符 + 指令 + 缓存数字」放在同一个 run 里 —— 浏览器的 docx 预览器会把**含域的整个 run 丢掉**（数字一起消失，页码位置错乱）。现改为 `begin / instrText / separate / 1 / end` 各自独立 run，数字 run 得以保留。

### 影响范围
- 所有用 `md2docx` 生成的 docx 的**纸张与页脚**（正文排版不变）。
